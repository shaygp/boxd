rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Profile pictures: fileName format is {userId}_{timestamp}.{ext}
    match /profile-pictures/{fileName} {
      allow read: if true;
      allow write: if request.auth != null
                   && fileName.matches('.*' + request.auth.uid + '_.*');
    }
  }
}
